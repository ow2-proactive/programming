<?xml version="1.0" encoding="UTF-8"?>
<project name="ProActive Documentation" basedir=".">
	
	<import file="common.xml" />
	
	<!-- ================================================================ -->
		<!--            Create docs, which is the javadoc + manual            -->
		<!--  =============================================================== -->
		<target name="docs" depends="javadoc.published, manual" description="Construct the javadoc and the manual" />
	
		<!-- ================================================================ -->
		<!--                     Create javadoc                               -->
		<!-- ================================================================ -->
		<target name="javadoc.all" depends="javadoc.published,javadoc.complete"/>


		<target name="javadoc.complete" depends="compile.util, deploy" description="Make complete Javadoc">
			<mkdir dir="${docs.tmp.dir}" />
			<javadoc  
				destdir="${docs.api.complete.dir}" 
				author="true" 
				version="true" 
				source="${source}" 
				use="true" 
				windowtitle="ProActive complete API" 
				breakiterator="yes" 
				additionalparam="-docfilessubdirs -excludedocfilessubdir CVS:test_documentation"> 

				<classpath>
						<path refid="compile.libs"/>
						<pathelement path="${cl.proactive.dir}"/>
						<pathelement path="${cl.extensions.dir}"/>
				</classpath>

				<packageset dir="${src.proactive.dir}" />
				<packageset dir="${src.extensions.dir}" />
				<packageset dir="${src.extra.dir}" />

				<link href="http://fractal.objectweb.org/current/doc/javadoc/fractal/" />
				<link href="http://java.sun.com/j2se/1.5.0/docs/api/"/>
				<doctitle><![CDATA[ProActive ${version}]]></doctitle>
				<bottom><![CDATA[<i>Copyright 1997-2008 INRIA All Rights Reserved.</i>]]></bottom>
	
				<group title="ProActive packages for end user" packages="org.objectweb.proactive" />
				<group title="ProActive Core" packages="org.objectweb.proactive.core*" />
				<group title="ProActive Core : Body" packages="org.objectweb.proactive.core.body*" />
				<group title="ProActive Core : Components (implementation of the Fractal model)" packages="org.objectweb.proactive.core.component*" />
				<group title="ProActive P2P : P2P infrastructure" packages="org.objectweb.proactive.extra.p2p*" />
				<group title="ProActive Extensions : Security" packages="org.objectweb.proactive.ext.security*" />
				<group title="ProActive Extensions" packages="org.objectweb.proactive.ext*" />
				<group title="ProActive Branch and Bound API" packages="org.objectweb.proactive.extra.branchnbound*" />
				<group title="ProActive Calcium API" packages="org.objectweb.proactive.calcium*" />
				<group title="ProActive Load Balancing API" packages="org.objectweb.proactive.loadbalancing*" />
				<group title="ProActive MPI API" packages="org.objectweb.proactive.mpi*" />
				<group title="ProActive OSGI API" packages="org.objectweb.proactive.osgi*" />
				<group title="ProActive JMX API" packages="org.objectweb.proactive.jmx*" />
				<group title="ProActive Scheduler API" packages="org.objectweb.proactive.scheduler*" />
				<group title="ProActive Benchmarks API" packages="org.objectweb.proactive.benchmarks*" />
					
				
			</javadoc>			
		</target>
	
		<target name="javadoc.published" depends="compile.util, deploy" description="Make published Javadoc">
			<mkdir dir="${docs.tmp.dir}" />
			<javadoc source="${source}"	classpathref="compile.libs">
				<doclet name="doc.PublishedAPIDoclet" path="${cl.utils.dir}">
					<param name="-file" value="${docs.published.coreclasses}" />
				</doclet>
				<packageset dir="${src.proactive.dir}"/>
			</javadoc>
			
			<javadoc source="${source}">
				<classpath>
					<path refid="compile.libs"/>
					<pathelement path="${cl.proactive.dir}"/>
				</classpath>
				<doclet name="doc.PublishedAPIDoclet" path="${cl.utils.dir}">
					<param name="-file" value="${docs.published.extensionsclasses}" />
				</doclet>
				<packageset dir="${src.extensions.dir}"/>
			</javadoc>
						
			<javadoc source="${source}">
				<classpath>
						<path refid="compile.libs"/>
						<pathelement path="${cl.proactive.dir}"/>
						<pathelement path="${cl.extensions.dir}"/>
				</classpath>
				<doclet name="doc.PublishedAPIDoclet" path="${cl.utils.dir}">
					<param name="-file" value="${docs.published.extraclasses}" />
				</doclet>
				<packageset dir="${src.extra.dir}"/>
			</javadoc>
						
			<javadoc  
				destdir="${docs.api.published.dir}" 
				author="true" 
				version="true" 
				source="${source}" 
				use="true" 
				windowtitle="ProActive API" 
				breakiterator="yes" 
				additionalparam="-docfilessubdirs -excludedocfilessubdir CVS:test_documentation"> 

				<classpath>
						<path refid="compile.libs"/>
						<pathelement path="${cl.proactive.dir}"/>
						<pathelement path="${cl.extensions.dir}"/>
						<pathelement path="${cl.extra.dir}"/>
				</classpath>

				
				<fileset dir="${src.proactive.dir}" includesfile="${docs.published.coreclasses}"/>
				<fileset dir="${src.extensions.dir}" includesfile="${docs.published.extensionsclasses}"/>
				<fileset dir="${src.extra.dir}" includesfile="${docs.published.extraclasses}"/>

				<link href="http://fractal.objectweb.org/current/doc/javadoc/fractal/" />
				<link href="http://java.sun.com/j2se/1.5.0/docs/api/"/>
				<doctitle><![CDATA[ProActive ${version}<br>]]></doctitle>
				<bottom><![CDATA[<i>Copyright 1997-2008 INRIA All Rights Reserved.</i>]]></bottom>
				
				<group title="ProActive entry point">
					<package name="org.objectweb.proactive.api" />
				</group>
					
				<group title="ProActive Core">
					<package name="org.objectweb.proactive.core*" />
					<package name="org.objectweb.proactive.core.filetransfer*" />
					<package name="org.objectweb.proactive.ext*" />
					<package name="org.objectweb.proactive.gcmdeployment*" />
					<package name="org.objectweb.proactive.mpi*" />  
					<package name="org.objectweb.proactive.benchmarks*" />
					<package name="org.objectweb.proactive" />
					<package name="org.objectweb.proactive.annotation" /> 
					
				</group>
								
				<group title="ProActive Extensions">
					<package name="org.objectweb.proactive.extensions.*" />
				</group>
							
				<group title="ProActive Extra">
					<package name="org.objectweb.proactive.extra.*" /> 
				</group>
				
			</javadoc>			
		</target>

	<!-- ==================================================================== -->
	<!--      The following docbook* are sub-tasks to build the Manual        -->
	<!-- ==================================================================== -->
	<!-- If a needed file doesn't exist, it will be fetched on the web -->	
	<xmlcatalog id="docbookcatalog">
			<!-- If a needed file doesn't exist, it will be fetched on the web -->

			<classpath>
				<!-- Here are some possible paths for the xsl stylesheets, which are contained in docbook-xsl-${docbook.version}.zip-->
				<pathelement location="${docs.docbook}/docbook-xsl-${docbook.version}.zip" />
			</classpath>

			<dtd publicId="-//OASIS//DTD DocBook XML V5.0//EN" location="${docs.docbook.dtd}" />
			<dtd publicId="http://www.oasis-open.org/docbook/xml/5.0b5/dtd/docbook.dtd"  location="${docs.docbook.dtd}"/>

			<entity publicid="http://docbook.sourceforge.net/release/xsl/${docbook.version}/profiling/profile.xsl" location="docbook-xsl-${docbook.version}/profiling/profile.xsl" />
			<entity publicid="http://docbook.sourceforge.net/release/xsl/${docbook.version}/html/chunk.xsl" location="docbook-xsl-${docbook.version}/html/chunk.xsl" />
			<entity publicid="http://docbook.sourceforge.net/release/xsl/${docbook.version}/html/docbook.xsl" location="docbook-xsl-${docbook.version}/html/docbook.xsl" />
			<entity publicid="http://docbook.sourceforge.net/release/xsl/${docbook.version}/fo/docbook.xsl" location="docbook-xsl-${docbook.version}/fo/docbook.xsl" />
		</xmlcatalog>

	
	


	<!-- ==================================================================== -->
	<!--      Converts docbook xml into ??? - uses the trax processor         -->
	<!-- ==================================================================== -->
	<target name="-docbookTransform"	>
		<!-- Profiling step: keep only the tags which have the attribute os="${docbook.profile}" or no os attribute -->
		<xslt basedir="${doc.src}"  in="${docs.tmp.dir}/${docbookInputFile}" out="${docs.tmp.dir}/profiled.xml" style="${docs.xsl.profiling}" force="yes">
			<param name="profile.os" expression="${docbook.profile}" />
			<!--docbook.profile can be html, pdf or empty-->
			<xmlcatalog refid="docbookcatalog" />
			<classpath>
				<fileset dir="${lib.dir}">
					<include name="**/*.jar" />
				</fileset>
				<fileset dir="${base.dir}/dev/lib">
					<include name="**/*.jar" />
				</fileset>
			</classpath>
		</xslt>

		<!-- Do the real conversion, docbook to pdf or html -->
		<xslt basedir="${doc.src}" in="${docs.tmp.dir}/profiled.xml" out="${docbookOutputFile}" style="${docbookStyle}" force="yes">
			<xmlcatalog refid="docbookcatalog" />
			<param name="TODAY" expression="${TODAY}" />
			<!-- the date for the first page -->
			<param name="VERSION" expression="${version}" />
			<!-- the version number, for the first page -->

			<!-- Output dir for the chunked stylesheet-->
			<param name="base.dir" expression="${docs.html.multiple.dir}"/>

			<classpath>
				<fileset dir="${lib.dir}">
					<include name="**/*.jar" />
				</fileset>
				<fileset dir="${base.dir}/dev/lib">
					<include name="**/*.jar" />
				</fileset>
			</classpath>
		</xslt>
	</target>
	
	<!-- ==================================================================== -->
	<!--                          Formats the XML files                       -->
	<!--            for now it just removes whitespaces from programlisting   -->
	<!--			(whitespaces are shown verbatim in the final doc)		  -->
	<!--                         	SIDE EFFECTS:                             -->
	<!--    due to the way XPath works it also removes DOCTYPEs and comments  --> 
	<!--  		              that are note not under the root	node          -->
	<!--         (ex: comments at the beginning or end of the file)            -->
	<!-- ==================================================================== -->
	<target name="formatXML">
		<!-- delete everything in the temp dir since we will
		run the xslt task and then copy everything back to 
		docs source -->
		<delete quiet="true">
			<fileset dir="${docs.tmp.dir}"/>
		</delete>
		<!--TODO REMOVE/MAKE UNNECESARY UGLY EXCLUDE by organizing doc source directory or in some other way -->
		<xslt basedir="${doc.src}"
		includes="**/*.xml"
		extension=".xml"
		destdir="${docs.tmp.dir}"
		 style="${docs.xsl.removenewlines}"
		 force="yes"
		 excludes="**/helloworld-distributed.xml **/temporary_gcm.xml **/temporary_gcm_code.xml
		 **/MPIRemote-descriptor.xml **/toadd.xml
		 **/MPI-Jacobi-nina-nef.xml **/web.xml 
		 **/deployment.xml **/PA_index.xml **/sample_p2p.xml **/application.xml" >
			<xmlcatalog refid="docbookcatalog" />

			<classpath>
				<fileset dir="${lib.dir}">
					<include name="**/*.jar" />
				</fileset>
				<fileset dir="${base.dir}/dev/lib">
					<include name="**/*.jar" />
				</fileset>
			</classpath>
		</xslt>
 		<!-- copy the processed files back -->
		<copy todir="${doc.src}"
		  verbose="true">
		    <fileset dir="${docs.tmp.dir}">
		      <include name="**/*.xml"/>
		    </fileset>
		  </copy>
		 <!-- clean tmp folder --> 
		 <delete quiet="true">
			<fileset dir="${docs.tmp.dir}"/>
		</delete>
	</target>
	
	<!-- ==================================================================== -->
	<!--          Generic task to transform docbook xml into pdf              -->
	<!-- ==================================================================== -->
	<target name="-docbookToPdf">
		<property name="fo.output" value="${pdfOutputFile}.fo" />

		<!--   First translate into intermediate fo format -->
		<antcall target="-docbookTransform">
			<param name="docbookOutputFile" value="${fo.output}" />
			<param name="docbookStyle" value="${docs.xsl.pdf}" />
			<param name="docbook.profile" value="pdf" />
		</antcall>

		<!-- declare how to transform fo xml file in pdf through a new 'fop' task -->
		<taskdef name="fop" classname="org.apache.fop.tools.anttasks.Fop" description="Official way to translate .fo (result of xml docbook translation) into .pdf">
			<classpath>
				<fileset dir="${docs.docbook}">
					<include name="*.jar" />
				</fileset>
				
			</classpath>
		</taskdef>

		<!-- Then use the newly defined fop task to translate fo into pdf -->
		<fop format="application/pdf" fofile="${fo.output}" outfile="${pdfOutputFile}" basedir="${doc.src}" />

		<delete file="${fo.output}" />
	</target>


	<!-- ==================================================================== -->
	<!--          Create the snippets necessary for programlistings           -->
	<!-- ==================================================================== -->
	<target name="-snippetscreation" depends="compile.util">
		<delete quiet="true">
			<fileset dir="${snippets.location}"/>
		</delete>
		<mkdir dir="${snippets.location}"/>
		<java classname="doc.snippets.Snippetizer" fork="true">
			<classpath refid="project.classpath"/>
			<!-- The root for parser start, it will check all the files in the dir and subdirs-->
			<arg value="${base.dir}" />
			<!-- The output path for snippets  -->
			<arg value="${snippets.location}" />
		</java>
	</target>
	
	
	
	<!-- ==================================================================== -->
	<!--    Add some highlighting information to the docbook source code      -->
	<!-- ==================================================================== -->
	<target name="-docbookBeautify" depends="compile.util">
		
		<java classname="doc.DocBookize" fork="true">
			<classpath refid="project.classpath"/>
			<!-- The xml file to parse -->
			<arg value="${docs.tmp.dir}/${docbookInputFile}" />
			<!-- The path to find java files in html -->
			<arg value="ProActive_src_html/${proactive.path}/" />
			
			<!-- the path for java file inclusion -->
			<arg value="${src.proactive.dir}/${proactive.path}/" />
			<arg value="${src.examples.dir}/${proactive.path}/" />
			
			<!-- the path for xml file inclusion -->
			<arg value="${descriptors.dir}/" />
			
		</java>

		<java classname="doc.Validate" fork="true">
			<classpath refid="project.classpath" />
			<classpath>
				<fileset dir="${docs.docbook}">
					<include name="*.jar" />
				</fileset>
			</classpath>
			<arg value="${docs.tmp.dir}/${docbookInputFile}" />
		</java>

	</target>

	<!-- ==================================================================== -->
	<!--    Checks if the XML is valid and if the manual can be built properly-->
	<!--   this should catch if the XML is malformed  or files are missing    -->
	<!-- ==================================================================== -->
	<target name="-docbookBeautifyFailCheck" depends="compile.util">
		
		<java classname="doc.DocBookize" fork="true" failonerror="true">
			<classpath refid="project.classpath"/>
			<!-- The xml file to parse -->
			<arg value="${docs.tmp.dir}/${docbookInputFile}" />
			<!-- The path to find java files in html -->
			<arg value="ProActive_src_html/${proactive.path}/" />
			
			<!-- the path for java file inclusion -->
			<arg value="${src.proactive.dir}/${proactive.path}/" />
			<arg value="${src.examples.dir}/${proactive.path}/" />
			
			<!-- the path for xml file inclusion -->
			<arg value="${descriptors.dir}/" />
			
		</java>

		<java classname="doc.Validate" fork="true" failonerror="true">
			<classpath refid="project.classpath" />
			<classpath>
				<fileset dir="${docs.docbook}">
					<include name="*.jar" />
				</fileset>
			</classpath>
			<arg value="${docs.tmp.dir}/${docbookInputFile}" />
		</java>

	</target>


	<!-- ==================================================================== -->
	<!--       XML Check by running everything up to the XSLT processor       -->
	<!-- ==================================================================== -->
	<target name="checkXML">

		<!-- Call the code for generating the snippets -->
		<antcall target="-snippetscreation"/>

		<!-- as this file gets edited, make sure it's a fresh copy-->
		<delete file="${docs.tmp.dir}/${docs.mainFile}" />

		<!-- Copy the files needed for the html files (pics and maybe others) -->
		<copy toDir="${docs.html.multiple.dir}">
			<fileset dir="${doc.src}">
				<include name="**/*.png" />
				<include name="**/*.jpg" />
				<include name="**/*.svg" />
				<include name="**/*.gif" />
				<include name="${docs.html.css.name}" />
			</fileset>
		</copy>

		<!-- Copying xml files so they can be edited (beautify programlistings) -->
		<copy toDir="${docs.tmp.dir}" includeemptydirs="no">
			<fileset dir="${doc.src}">
				<include name="**/*" />
				<exclude name="**/.svn" />
				<exclude name="xsl" />
			</fileset>
		</copy>

		<!-- Decorate the code a bit (code examples in programlistings) -->
		<antcall target="-docbookBeautifyFailCheck">
			<param name="docbookInputFile" value="${docs.mainFile}" />
		</antcall>

	</target>

	<!-- ==================================================================== -->
	<!-- preprocessing for the manual: copy files and add tags to source code -->
	<!-- ==================================================================== -->
	<target name="-docbookPreProcess">

		<!-- Call the code for generating the snippets -->
		<antcall target="-snippetscreation"/>

		<!-- as this file gets edited, make sure it's a fresh copy-->
		<delete file="${docs.tmp.dir}/${docs.mainFile}" />

		<!-- Copy the files needed for the html files (pics and maybe others) -->
		<copy toDir="${output.docs.dir}" includeemptydirs="no" >
			<fileset dir="${doc.src}">
				<include name="**/*.png" />
				<include name="**/*.jpg" />
				<include name="**/*.svg" />
				<include name="**/*.gif" />
				<include name="${docs.html.css.name}" />
			</fileset>
		</copy>

		<!-- Copying xml files so they can be edited (beautify programlistings) -->
		<copy toDir="${docs.tmp.dir}" includeemptydirs="no">
			<fileset dir="${doc.src}">
				<include name="**/*" />
				<exclude name="**/.svn" />
				<exclude name="xsl" />
			</fileset>
		</copy>

		<!-- Decorate the code a bit (code examples in programlistings) -->
		<antcall target="-docbookBeautify">
			<param name="docbookInputFile" value="${docs.mainFile}" />
		</antcall>

	</target>


	<!-- ==================================================================== -->
	<!--          Make just the html files for the manual                     -->
	<!-- ==================================================================== -->
	<target name="manualHtml" description="Make only the html files in the manual">
		<antcall target="-docbookPreProcess">
				<param name="output.docs.dir"  value="${docs.html.multiple.dir}" />
		</antcall>
		<antcall target="-docbookTransform">
			<param name="docbookInputFile" value="${docs.mainFile}" />
			<param name="docbookOutputFile" value="${docs.html.multiple.output}" />
			<param name="docbookStyle" value="${docs.xsl.html.chunked}" />
			<param name="docbook.profile" value="html" />
		</antcall>
	</target>
	<!-- ==================================================================== -->
	<!--   Make an archive from the files for the multiple html manual        -->
	<!-- ==================================================================== -->
	<target name="manualHtmlZip" description="Create a zip of the html filed in the manual" depends="manualHtml">
		<zip destfile="${docs.html.multiple.zip}" basedir="${docs.html.multiple.dir}" excludes="${docs.html.single.name}"/>
	</target>

	<!-- ==================================================================== -->
	<!--               Make just the pdf files for the manual                 -->
	<!-- ==================================================================== -->
	<target name="manualPdf" description="Make only the pdf files in the manual">
		<antcall target="-docbookPreProcess">
			<param name="output.docs.dir"  value="${docs.pdf.dir}" />
		</antcall>
		<antcall target="-docbookToPdf">
			<param name="docbookInputFile" value="${docs.mainFile}" />
			<param name="pdfOutputFile" value="${docs.pdf.output}" />
		</antcall>
		<!-- remove all files except the PDF -->
		<!--  needed because docbookPreProcess copies all the files -->
		<delete includeemptydirs="true">
			<fileset dir="${docs.pdf.dir}" defaultexcludes="false"
			 	includes="**/*"  excludes="${docs.pdf.name}" />
		</delete>
	</target>
	<!-- ==================================================================== -->
	<!--           Make only one huge html-file from the doc                  -->
	<!-- ==================================================================== -->
	<target name="manualSingleHtml" description="Make only the big html file in the manual">
		<antcall target="-docbookPreProcess">
			<param name="output.docs.dir"  value="${docs.html.single.dir}" />
		</antcall>
		<antcall target="-docbookTransform">
			<param name="docbookInputFile" value="${docs.mainFile}" />
			<param name="docbookOutputFile" value="${docs.html.single.output}" />
			<param name="docbookStyle" value="${docs.xsl.html.one}" />
			<param name="docbook.profile" value="html" />
		</antcall>
	</target>
	<!-- ==================================================================== -->
	<!--      Make an archive from the files for the single html manual       -->
	<!-- ==================================================================== -->
	<target name="manualSingleHtmlZip" description="Create a zip of the big html file in the manual" depends="manualSingleHtml">
		<zip destfile="${docs.html.single.zip}" basedir="${docs.html.single.dir}" includes="${docs.html.css.name} ${docs.html.single.name} **/*.gif **/*.png **/*.jpg"/>
	</target>

	<!-- ==================================================================== -->
	<!-- Construct the manual from the XML files in {docs.src}                -->
	<!-- ==================================================================== -->
	<target name="manual" description="Build all the different manual version: html, pdf... " depends="manualSingleHtml,manualHtml,manualPdf">
		<!-- The generation of the files is done by the depends clause -->
		<!-- Once the doc files have been generated, do some cleaning up -->
		<delete dir="${docs.tmp.dir}" />
		<copy todir="${docs.dir}/examples">
			<!-- XML descriptor examples also available -->
			<fileset dir="${base.dir}/descriptors/examples" />
		</copy>
	</target>
</project>
